server {
    listen 80;
    server_name ai.deard.cloud;
    # HTTP 强制跳转 HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name ai.deard.cloud;

    # SSL 证书路径
    ssl_certificate     /etc/nginx/certs/ai.deard.cloud_nginx/ai.deard.cloud_bundle.crt;
    ssl_certificate_key /etc/nginx/certs/ai.deard.cloud_nginx/ai.deard.cloud.key;

    # SSL 优化配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # 反向代理转发到 47.82.123.72:10001 端口
    location / {
        proxy_pass http://47.82.123.72:10001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # 超时配置
        proxy_connect_timeout 60s;
        proxy_read_timeout 60s;
    }
}
